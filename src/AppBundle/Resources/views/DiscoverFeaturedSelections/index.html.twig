{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <div class="page page--featured-selection">
        {% include 'AppBundle:_Includes:navigation-secondary.html.twig' %}
        <div class="page__content">
            <div class="box">
                <div class="wrapper">
                    <div class="content">
                        <div class="box__list">
                            {% for i in 1..10 %}
                                {% include 'AppBundle:DiscoverFeaturedSelections:_recent-features-item.html.twig' %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>


{% endblock body %}

